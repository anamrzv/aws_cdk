{
  "name": "lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "npm i",
    "compile": "npx tsc",
    "zip": "zip -r name-lambda.zip dist/ node_modules/",
    "compile:zip": "npm run compile && npm run zip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "aws-sdk": "^2.1691.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/aws-sdk": "^0.0.42",
    "@types/node": "^22.8.2",
    "typescript": "^5.6.3"
  }
}
